<Window x:Class="DMXForGamers.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DMXForGamers"
        xmlns:views="clr-namespace:DMXForGamers.Views"
        xmlns:monitors="clr-namespace:DMXForGamers.Views.Monitors"
        xmlns:common="clr-namespace:DMXForGamers.Views.Common"
        xmlns:converters="clr-namespace:DMXForGamers.Converters"
        mc:Ignorable="d"
        Title="DMX For Gamers" Height="600" Width="800" Icon="DMXForGamers.ico" Closed="Window_Closed" Closing="Window_Closing">
    <Window.Resources>
        <converters:ListToStringConverter x:Key="listToStringConverter" />
    </Window.Resources>
    <Grid Margin="5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="5"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="5"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="5"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="5"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="5"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <TextBlock Grid.Row="0">Protocol:</TextBlock>
        <ComboBox Grid.Row="0" Grid.Column="2" ItemsSource="{Binding Path=Protocols}" DisplayMemberPath="Name" SelectedItem="{Binding Path=SelectedProtocol}"></ComboBox>
        <TabControl Grid.Row="2" Grid.ColumnSpan="3" SelectedIndex="{Binding Path=SelectedMonitorIndex}">
            <TabItem Header="New Process Monitor">
                <monitors:NewProcessMonitor DataContext="{Binding Path=NewProcessMonitor}"></monitors:NewProcessMonitor>
            </TabItem>
            <TabItem Header="File Monitor">
                <monitors:FileMonitor DataContext="{Binding Path=FileMonitor}"></monitors:FileMonitor>
            </TabItem>
            <TabItem Header="No Monitor">
                <TextBlock>Manual triggering of events only...</TextBlock>
            </TabItem>
        </TabControl>
        <TextBlock Grid.Row="4">Events File:</TextBlock>
        <common:FileTextBox Grid.Row="4" Grid.Column="2" FilePath="{Binding Path=EventsFile}"></common:FileTextBox>
        <TextBlock Grid.Row="6">DMX File:</TextBlock>
        <common:FileTextBox Grid.Row="6" Grid.Column="2" FilePath="{Binding Path=DMXFile}"></common:FileTextBox>
        <TabControl Grid.Row="8" Grid.ColumnSpan="3">
            <TabItem Header="Main Control">
                <Grid Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="10"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <Button Grid.Row="0" Grid.Column="0" Click="StartButton_Click" Name="_startButton">Start</Button>
                    <Button Grid.Row="0" Grid.Column="2" Click="StopButton_Click" Name="_stopButton">Stop</Button>
                    <ScrollViewer Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="3" x:Name="m_OutputScroll">
                        <TextBlock Text="{Binding Path=Output, Converter={StaticResource listToStringConverter}}"></TextBlock>
                    </ScrollViewer>
                </Grid>
            </TabItem>
            <TabItem Header="Manual Triggering">
                <Grid>
                    <views:ManualEvents></views:ManualEvents>
                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</Window>
